from ._Annotation import Annotation


class ImageAnnotation(Annotation['ImageAnnotation']):
    """
    Represents a single annotation in an image.
    """
    # No additional properties required
    pass
